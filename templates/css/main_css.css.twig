body {
    {{ mainCss.body|raw }}
}

a {
    {{ mainCss.link['base']|raw }};
}

a:hover {
    {{ mainCss.link['hover']|raw }};
}
p{
    {{ mainCss.paragraph|raw }};
}

header,
#text,
#video,
#photo,
#card,
#bandcamp-album,
#on-the-internet,
#share,
footer {
        {{ mainCss.initSections|raw }};
}

header {
{{ mainCss.header['header']|raw }};
}

header h1 {
{{ mainCss.header['h1']|raw }};
}

header h2 {
{{ mainCss.header['h2']|raw }};
}