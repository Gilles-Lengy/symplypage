body {
{{ mainCss.body|raw }}
}

a {
{{ mainCss.link['base']|raw }};
}

a:hover {
{{ mainCss.link['hover']|raw }};
}

p {
{{ mainCss.paragraph|raw }};
}

header,
#text,
#video,
#photo,
#card,
#bandcamp-album,
#on-the-internet,
#share,
footer {
{{ mainCss.initSections['section']|raw }};
}

#video h3,
#photo h3,
#card h3,
#bandcamp-album h3,
#on-the-internet h3,
#share h3 {
{{ mainCss.initSections['h3']|raw }};
}

.card {
{{ mainCss.initCard|raw }};
}

header {
{{ mainCss.header['header']|raw }};
}

header h1 {
{{ mainCss.header['h1']|raw }};
}

header h2 {
{{ mainCss.header['h2']|raw }};
}

#text {
{{ mainCss.textSection['section']|raw }};
}

#text h2{
{{ mainCss.textSection['h2']|raw }};
}

#text p{
{{ mainCss.textSection['p']|raw }};
}

#text p#text-signature{
{{ mainCss.textSection['pTextSignature']|raw }};
}

#text p#text-signature a{
{{ mainCss.textSection['pTextSignatureLink']|raw }};
}

#text p#text-signature a:hover{
{{ mainCss.textSection['pTextSignatureLinkHover']|raw }};
}