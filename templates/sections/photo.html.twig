<section id="photo">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h3>{{ symplyPage.sectionPhotoTitle }}</h3>

                {% for PhotoData in symplyPage.sectionPhotoData %}
                    <div class="card">
                        <img class="card-img-top" src="{{ PhotoData[0] }}"
                             alt="{{ PhotoData[1] }}"/>
                        <div class="card-body">
                            <h5 class="card-title">{{ PhotoData[1] }}</h5>
                            <p class="card-text">{{ PhotoData[2] }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
</section>